#include <stdio.h>

#define minfinder(a,b) a<b ? a:b

int main() {
	char board[50][50];

	int N, M;
	int min = 80;
	int cntB = 0, cntW = 0;

	scanf("%d %d", &N, &M);
	for (int i = 0; i < N; i++)
		scanf("%s", &board[i]);
	for (int i = 0; i < N - 7; i++) {
		for (int j = 0; j < M - 7; j++) {
			cntB = 0;
			cntW = 0;
			for (int a = i; a < i + 8; a++) {

			}
		}
	}
}
